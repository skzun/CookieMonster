# CookieMonster — allowlist de escopo (scope guardrail)
#
# Liste aqui os dominios/hosts autorizados para replay/check. Um dominio fora
# desta lista e RECUSADO pela ferramenta (fail-safe). Para desabilitar este
# guardrail em ambiente controlado, use --allow-unsafe-scope (NAO recomendado).
#
# Para alvos reais autorizados, adicione manualmente aqui (um por linha).
# Linhas iniciadas por '#' sao comentarios.

# Laboratorio local
127.0.0.1
localhost
lab.example.test

# Alvos de teste autorizados (dados do laboratorio do usuario):
amazon.com
github.com
steamcommunity.com
spotify.com
netflix.com